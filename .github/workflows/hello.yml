name: Hello World Training Workflow

on:
  workflow_dispatch:

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Greet the User
        run: echo "Hello World!"
      - name: Greet the User
        run: echo "Going to list the directory contents!"
      - run: ls -l
      - uses: actions/checkout@v5
      - run: ls -l
